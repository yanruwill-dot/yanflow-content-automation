# 验证报告

日期：2026-07-26

## 自动化测试

- Python 单元与 HTTP 测试：27 项通过。
- JavaScript 语法检查：通过。
- Python 编译检查：通过。
- Shell 启动脚本语法检查：通过。
- 覆盖的高风险规则：
  - 写接口必须有本地会话令牌。
  - 正式发布前必须通过 Dry-run。
  - 公开文案内部标签、站外导流和绝对化承诺拦截。
  - 相同图片感知哈希拦截。
  - 单账号每日上限和最短间隔拦截。
  - 定时配置不能打开正式发布。
  - 公众号预览使用隔离 CSP，禁止脚本和表单提交。
  - RSS 与 Atom 公开来源解析。
  - 任务进度明细对前端可见。
  - 蚁小二上传结果保留小红书必需的图片格式字段。
  - 新建任务保存每个平台账号与排版。
  - 账号或排版变化强制作废旧 Dry-run，并改变内容指纹。
  - 外部任务号产生后禁止切换账号或排版。
  - 三套公众号预览都使用隔离、自适应样式并删除活动脚本。
  - 服务重启后会把贴图工作台已失败的任务同步为失败，不再显示假“配图中”。

## Codex 内置浏览器

- 首页、内容工厂、风控中心、设置与连接页面正常显示。
- 真实试跑任务：`20260726-143556-3380d09e`，内容方向“效率”。
- 抓取并保留 18 条公开来源，生成 5 个候选选题。
- 生成题目《AI效率的下一步，不是更会聊，而是更会执行》，内容质量 93 分。
- 使用 Codex 原生 Image2 生成 1 张 1086×1448 的 3:4 成品图。
- 小红书与微信公众号均通过 yxer 3.1.3 完整 Dry-run，未正式提交。
- 页面实时显示：总流程 92%、当前阶段 100%、5/5 步。
- 页面显示 5 个选题、93 分正文和 1 张配图；浏览器控制台无 error/warn。
- 初测发现静态脚本缓存导致进度区仍显示 0%；改为 `no-store` 并加版本参数后复测通过。

## 响应式布局

- 修复前，390px 手机视口的页面宽度达到 1861px，横向溢出 1471px。
- 修复后，360px、390px、768px、1440px、2560px 视口的横向溢出均为 0。
- 360px/390px：顶部导航、两列指标、单列表单、两列流水线、单列内容和风控卡片正常。
- 768px：侧栏导航保留，指标两列、内容与设置单列，没有裁切。
- 1440px/2560px：工作区完整利用网页宽度，不再只显示左侧小块内容。
- 内容工厂、风控中心、定时与连接三个页面均完成手机端点击与显示检查。
- 单张 Image2 配图在手机端以 280px 宽度居中显示。
- 响应式实测后浏览器控制台无 error/warn。
- 账号实测读取到两个小红书账号和一个公众号账号；新建任务与当前任务切换控件均可用。
- 390px 手机视口下账号与排版改为单列，页面宽度与视口同为 390px；1440px 桌面视口同样无横向溢出。

## 剩余真实边界

- 新内容全流程会调用外部来源、Image2 和蚁小二，耗时与平台状态受外部服务影响。
- 账号即使当前可用，平台仍可能在正式提交时要求验证码、实名或重新登录。
- “低封禁风险”是工程控制目标，不是平台结果保证。
- 本轮账号与排版功能测试没有再次正式发表；只做本地选择、接口、预览和响应式验证。

## 安装包复验

- 最新 ZIP 只包含源码、静态页面、测试和说明，共 18 个条目；不包含 `runtime/jobs`、发布回执或账号历史。
- 解压到隔离临时目录后，27 项测试、JavaScript 语法和 Python 编译再次通过。
- ZIP 的最终 SHA-256 在交付回复中单独给出。
